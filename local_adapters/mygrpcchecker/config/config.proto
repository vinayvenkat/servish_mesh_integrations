syntax = "proto3";

// config for mygrpcchecker
package adapter.mygrpcchecker.config;

import "gogoproto/gogo.proto";

option go_package="config";

// config for mylistchecker
message Params {
    // Path of the file to save the information about runtime requests.
    string file_path = 1;
}
