syntax = "proto3";

// config for mygrpcadapter
package adapter.mylogadapter.config;

import "gogoproto/gogo.proto";

option go_package="config";

// config for mylogadapter
message Params {
    // Path of the file to save the information about runtime requests.
    string file_path = 1;
}
